package racingcar.controller;

import org.junit.jupiter.api.Test;
import racingcar.domain.model.Race;

import racingcar.view.InputView;
import racingcar.view.OutputView;


import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

class RaceControllerTest {
//    void 문자가_입력되면_예외가_발생한다() {
//        // given
//        Race mockRace = mock(Race.class);
//        InputView mockInputView = mock(InputView.class);
//        OutputView mockOutputView = mock(OutputView.class);
//        when(mockInputView.inputTryNumber()).thenReturn("1");
//        RaceController controller = new RaceController();
//
//        assertThrows(IllegalArgumentException.class, controller::execute);
//    }
}